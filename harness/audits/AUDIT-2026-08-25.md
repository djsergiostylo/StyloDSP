# AUDIT — 2026-08-25

## Scope

Second-radiography / reconciliation baseline. This record captures confirmed observations already established from repository, CI and project-history inspection. It is intentionally not the final branch-by-branch reconciliation.

## Confirmed

- `main` contains the active Android implementation.
- The current player engine implements a real MediaExtractor → MediaCodec → PCM → EQ → FFT → SafetyLimiter → AudioTrack path.
- The engine exposes load, play/pause, seek, loop, bypass, volume and dynamic EQ-band updates.
- GitHub Actions produced an Android release artifact from commit `890a97a1d05448ff0d858aa46bb84fda8b4a4853`.
- A prior runtime test exposed a native-library packaging/runtime problem involving `libstylo_dsp_core.so`.
- Multiple historical/prototype branches exist.
- Rust Core is not currently established as the canonical DSP implementation on `main`.

## Contradictions corrected in this documentation branch

- Older documentation describes an earlier Android architecture and must not override current source.
- Older Harness state incorrectly described the Android tree as absent from `main`.
- `harness/features.json` had previously marked H-002 as PASSING despite unresolved reconciliation/runtime work. H-002 is now `IN_PROGRESS`.
- Rust architecture documents must be read as current only where the source tree and validation evidence support them.

## AI-memory improvement

The repository now has an explicit AI onboarding layer:

- `AI_START_HERE.md`
- `AI_CONTEXT.md`
- `AGENTS.md` onboarding rules
- operational Harness state, roadmap, decisions and validation documents
- dated audit records

This converts important project context from temporary conversation memory into durable repository memory.

## Not yet complete

- Full branch-by-branch comparison.
- Full inventory of branch-only functionality.
- Full documentation classification.
- Current APK installation/runtime validation.
- Final ChatGPT-history-to-GitHub reconciliation.
- Final branch disposition.

## Result

The project is not to be restarted. It is to be reconciled, validated and then cleaned incrementally.
