# STYLO HARNESS · VERIFICATION

This file is retained as a compatibility pointer for the original verification ladder.

## Canonical source
The current verification rules live in `harness/VALIDATION_PROTOCOL.md`.

The historical ladder was: Static → Build → Unit/Integration → Runtime → Parity. Those levels remain useful, but acceptance status must follow the canonical validation protocol and evidence rules.

**Rule:** Do not add a second verification policy here. Update `harness/VALIDATION_PROTOCOL.md` instead.
